# 일일 보고 (2026-01-10)

## 금일 작업 내용

1. **로그인/회원가입 및 세션 기반 인증 흐름 구축**
    * 세션 관리와 CSRF 보호를 포함한 인증 흐름을 구성
    * 회원가입/로그인 로직과 관련 유틸 정비

2. **사용자 프로필 조회/권한 처리 보강**
    * 사용자 프로필 접근 제어 강화 및 관련 라우트 구조 개선
    * 기존 사용자 관련 로직 일부 정리 및 통합

3. **게시판 CRUD 및 권한 체크 강화**
    * 게시글 생성/수정/삭제에 대한 권한 체크 로직을 정비
    * 게시판 서비스/컨트롤러 레이어 확장

4. **파일/이미지 업로드/삭제 기능 도입**
    * 대상
        * 게시글(`/board/:slug/:displayID`): 이미지 1개, 문서 1개
        * 아바타(`/settings/profile`): 이미지 1개
    * 이미지/파일 CRUD 처리 로직 추가
    * 업로드 및 파일 저장 경로 정리
    * 업로드 검증, 세션 반영, UI 반영 구현

5. **게시판 접근 이슈 수정**
    * `/board/:slug` 접근 오류 해결
    * `header.ejs` 등 `views` 파일 내 `href`링크 최신화
    * 기타 `Routes`/`services` 리팩터링 진행

## 차후 계획 중인 작업

1. **`Docker` && `Docker-Compose` 설정**
2. `production` 환경에서 Nginx 도입
3. `errors` 로직 중앙화 및 단일화
4. `favicon` 생성 및 설정
    * `UI/UX` 개선
    * 프로젝트의 **상징성 강화**
5. `.html` & `.ejs` 파일 `<title>` 전면 개편 필요
    * 현재 전부 `Document` **기본값으로 설정됨**
